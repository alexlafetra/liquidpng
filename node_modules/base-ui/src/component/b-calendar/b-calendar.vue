<!-- @author mengpeng -->
<!-- @email pengmeng@yangqianguan.com -->
<!-- @date 2019-07-25 16:24:16.302 -->
<!-- @desc generated by yqg-cli@1.0.9 -->

<template>
    <div class="b-calendar">
        <div
            :class="{'calendar-card':card}"
            class="b-calendar-header">
            <template v-if="!card">
                <div >
                    <slot
                        :data="date"
                        name="header-left">{{ date }}</slot>
                </div>
                <div>
                    <slot
                        :data="title"
                        name="header-center">{{ title }}</slot>
                </div>
            </template>

            <div class="b-calendar-button-group">
                <slot
                    name="header-right">
                    <b-button :size="ButtonSize" @click="changeDate('prev-month')">上个月</b-button>
                    <b-button :size="ButtonSize" @click="changeDate('today')">今天</b-button>
                    <b-button :size="ButtonSize" @click="changeDate('next-month')">下个月</b-button>
                </slot>
            </div>
        </div>
        <div
            :class="{'calendar-card':card}"
            class="b-calendar-body">
            <date-table
                :first-day-of-week="firstDayOfWeek"
                :timestamp="timestamp"
                @on-select-date="onSelect"
            />
        </div>
    </div>
</template>

<script type="text/babel">
import {genDateStringWithFormat, isValidDateProps} from '../../util/time';
import {changeOption} from './utils';
import DateTable from './date-table.vue';

export default {
    name: 'BCalendar',

    components: {
        DateTable
    },

    props: {
        title: {
            type: String,
            default: 'calendar'
        },

        value: {
            type: [Date, String, Number],
            default: () => new Date(),
            validator: isValidDateProps
        },

        firstDayOfWeek: {
            type: Number,
            default: 0,
            validator: (value) => {
                if (value < 7 && value >= 0) {
                    return Math.floor(value) === value;
                }
                return false;
            }
        },

        card: {
            type: Boolean,
            default: false
        }
    },

    provide() {
        return {
            bCalendar: this
        };
    },

    data() {
        const {value} = this;
        const time = new Date(value);
        return {
            timestamp: time.getTime()
        };
    },

    computed: {
        date() {
            return genDateStringWithFormat(this.timestamp, 'YYYY年MM月');
        },

        ButtonSize() {
            return this.card ? 'sm' : 'md';
        }
    },

    methods: {
        changeDate(period) {
            const vm = this;
            const {timestamp} = vm;
            vm.timestamp = changeOption[period](timestamp);
        },

        onSelect(val) {
            this.timestamp = val.timestamp;
        }
    }
};
</script>
